<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NewClockStyle">
        <item name="clock">@drawable/clock</item>
        <item name="center">@drawable/center</item>
        <item name="hour">@drawable/hour</item>
        <item name="minute">@drawable/minute</item>
        <item name="second">@drawable/second</item>
    </style>
</resources>
